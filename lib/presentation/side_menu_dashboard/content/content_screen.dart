import 'package:flutter/material.dart';

class ContentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Pantalla de Contenido', style: TextStyle(fontSize: 24)),
    );
  }
}
